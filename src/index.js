import React from 'react';
import ReactDOM from 'react-dom/client';
import AncestryApp from './AncestryApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AncestryApp />
  </React.StrictMode>
);